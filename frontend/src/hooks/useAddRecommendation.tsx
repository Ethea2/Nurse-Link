import { useRef } from "react"
import { Id, toast } from "react-toastify"
import axios from "axios"

const useAddRecommendation = () => {
    // code to successfully post recommendation
}

export default useAddRecommendation